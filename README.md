# nextjs-essentials
Learning nextjs essentials

1. Install dependencies:
> npm install

2. Run developing localhost:
> npm run dev

3. Open the page:
Go to the localhost adress from the terminal. (ex. http://localhost:3000/)
